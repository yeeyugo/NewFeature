<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yee.feature">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewFeature">
        <activity android:name=".ui.activity.CategoryActivity"></activity>
        <activity android:name=".ui.activity.SmartRefreshActivity" />
        <activity android:name=".ui.activity.StaggeredActivity" />
        <activity android:name=".ui.activity.GridActivity" />
        <activity android:name=".ui.activity.HeaderActivity" />
        <activity android:name=".ui.activity.RecyclerActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>